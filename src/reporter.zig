const std = @import("std");

// var reporter = Reporter.init(allocator, &system);
// defer reporter.deinit();
//
// try reporter.addPosReporter(file_name, .XYZ, 20);
// try reporter.addVelReporter(file_name, .VEL, 20);
// try reporter.addLogReporter(file_name, .LOG, 20);
//
// try reporter.reportPos(current_time);
// try reporter.reportVel(current_time);
// try reporter.reportLog(current_time);
//
// try reporter.report(current_time);
//

pub const Reporter = struct {};
